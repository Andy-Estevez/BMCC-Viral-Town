using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DuplexBlueScript : BuildingTemplate
{
    // Start is called before the first frame update
    void Start()
    {
        capacity = 20;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
